#include <string.h>
#include <stdio.h>

char ** parse_args( char * line ){
  char* args[5];
  args[0] = strsep(&line, " ");
  printf("%s", args[0]);
  /*
  for(int i; line; i++){
    args[i] = strsep(&line, " ");
  }
  */
  // printf("%s", args[0]);
  // printf("%s", args[1]);
}

int main(){
  char line[5] = "-l -a"
  char * args = "-l -a";
  parse_args(args);
}
